# globuy instruction bootstrap

The canonical repository instructions live in `codex/AGENTS.md` and `codex/codex.md`.
Before any work in this repository, read both files completely, then read
`docs/project-status.md` and `docs/vector-infrastructure.md`.

After every meaningful project or design change, update `docs/project-status.md` in the same turn.
Do not record secrets, fabricate external data, or substitute technologies that the canonical
documents mark as fixed without the user's explicit approval.

